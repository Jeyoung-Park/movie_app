import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); // ->component, jsx
// react native는 한 번에 하나의 컴포넌트만 렌더링 가능
